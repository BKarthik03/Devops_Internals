pipeline {
  agent any
  environment {
    BRANCH_NAME='dev'
  }
  stages {
    stage('Check branch') {
      steps {
        if(env.BRANCH_NAME=='dev'){
          echo env.BRANCH_NAME
        }
        echo "Hello World"
        
      }
    }
  }
}
